# Copy to .env for local dev (optional — dev defaults to /api proxy).
# For PRODUCTION (Vercel → Settings → Environment Variables), set:
#   VITE_API_URL=https://your-backend-name.onrender.com/api
VITE_API_URL=http://127.0.0.1:5000/api
